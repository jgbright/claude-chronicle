<section id="quickstart" class="quickstart reveal">
  <div class="section-header">
    <p class="kicker">Quick Start</p>
    <h2>Install in minutes on Linux, macOS, or Windows</h2>
    <p>Pick a platform, run the command, then launch the local app with <code>serve</code>.</p>
  </div>

  <div class="quickstart-grid">
    <article class="platform-card">
      <h3>Linux (amd64)</h3>
      <div class="code-block">
<pre><span class="comment"># download + unpack</span>
<span class="cmd">curl -fL https://github.com/jgbright/claude-chronicle/releases/latest/download/claude-chronicle_linux_amd64.tar.gz | tar xz</span>

<span class="comment"># run local UI</span>
<span class="cmd">./claude-chronicle serve</span></pre>
      </div>
    </article>

    <article class="platform-card">
      <h3>macOS (arm64)</h3>
      <div class="code-block">
<pre><span class="comment"># download + unpack</span>
<span class="cmd">curl -fL https://github.com/jgbright/claude-chronicle/releases/latest/download/claude-chronicle_darwin_arm64.tar.gz | tar xz</span>

<span class="comment"># run local UI</span>
<span class="cmd">./claude-chronicle serve</span></pre>
      </div>
    </article>

    <article class="platform-card">
      <h3>Windows (PowerShell)</h3>
      <div class="code-block">
<pre><span class="comment"># download release archive</span>
<span class="cmd">iwr -Uri https://github.com/jgbright/claude-chronicle/releases/latest/download/claude-chronicle_windows_amd64.zip -OutFile claude-chronicle.zip</span>

<span class="comment"># unpack + run</span>
<span class="cmd">Expand-Archive .\claude-chronicle.zip -DestinationPath .\claude-chronicle</span>
<span class="cmd">.\claude-chronicle\claude-chronicle.exe serve</span></pre>
      </div>
    </article>
  </div>

  <div class="next-step">
    <p>Export your first curated session:</p>
    <div class="code-inline">
      <code>claude-chronicle export -session &lt;id&gt; -o session.html</code>
    </div>
  </div>
</section>

<style>
  .quickstart {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--section-padding);
  }

  .section-header {
    margin-bottom: 1.2rem;
    max-width: 70ch;
  }

  .kicker {
    color: var(--color-link);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.75rem;
    font-weight: 700;
    margin-bottom: 0.35rem;
  }

  .section-header h2 {
    font-size: clamp(1.5rem, 3.7vw, 2.1rem);
    line-height: 1.15;
    margin-bottom: 0.5rem;
  }

  .section-header p {
    color: var(--color-text-muted);
  }

  .quickstart-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.95rem;
  }

  .platform-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 14px;
    box-shadow: var(--shadow-soft);
    padding: 0.9rem;
  }

  .platform-card h3 {
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
  }

  .code-block {
    background: #0f1728;
    border: 1px solid #1f2e4f;
    border-radius: 12px;
    padding: 0.8rem;
    overflow-x: auto;
  }

  pre {
    font-family: var(--font-mono);
    font-size: 0.76rem;
    color: #deebff;
    line-height: 1.55;
    white-space: pre;
  }

  .comment {
    color: #94abd0;
  }

  .cmd {
    color: #8ecbff;
  }

  .next-step {
    margin-top: 1rem;
    background: var(--color-surface-soft);
    border: 1px dashed var(--color-border-strong);
    border-radius: 12px;
    padding: 0.8rem 0.95rem;
  }

  .next-step p {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    margin-bottom: 0.45rem;
  }

  .code-inline code {
    display: inline-block;
    word-break: break-word;
  }

  @media (max-width: 1060px) {
    .quickstart-grid {
      grid-template-columns: 1fr;
    }

    pre {
      font-size: 0.8rem;
    }
  }
</style>