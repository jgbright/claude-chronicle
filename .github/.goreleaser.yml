version: 2
builds:
  - main: ./cmd/chronicle
    binary: claude-chronicle
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
archives:
  - formats: [tar.gz]
    name_template: "claude-chronicle_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
release:
  mode: replace
  extra_files:
    - glob: dist/demo-session.html
changelog:
  sort: asc
